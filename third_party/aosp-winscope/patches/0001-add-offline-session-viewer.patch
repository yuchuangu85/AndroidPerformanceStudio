diff --git a/winscope/src/app/components/app_component.ts b/winscope/src/app/components/app_component.ts
index 934fcace8..ab98016bb 100644
--- a/winscope/src/app/components/app_component.ts
+++ b/winscope/src/app/components/app_component.ts
@@ -53,6 +53,7 @@ import {getRootUrl} from 'common/window';
 import {CrossToolProtocol} from 'cross_tool/cross_tool_protocol';
 import {Analytics} from 'logging/analytics';
 import {ProgressListener} from 'messaging/progress_listener';
+import {NotificationType, UserNotification} from 'messaging/user_notification';
 import {
   AppFilesCollected,
   AppFilesUploaded,
@@ -344,11 +345,6 @@ import {RequestData} from 'cross_tool/g3_proxy';
               </h1>
 
               <div class="card-grid landing-grid">
-                <collect-traces
-                  class="collect-traces-card homepage-card"
-                  [storage]="appStorage"
-                  (filesCollected)="onFilesCollected($event)"></collect-traces>
-
                 <upload-traces
                   #uploadTraces
                   class="upload-traces-card homepage-card"
@@ -657,6 +653,41 @@ export class AppComponent implements WinscopeEventListener {
 
   async ngAfterViewInit() {
     await this.mediator.onWinscopeEvent(new AppInitialized());
+    this.mediator.setUploadTracesComponent(this.uploadTracesComponent);
+    await this.loadPackagedEvidence();
+  }
+
+  private async loadPackagedEvidence() {
+    const session = new URLSearchParams(window.location.search).get('session');
+    if (!session) return;
+    try {
+      const response = await fetch(
+        `./evidence?session=${encodeURIComponent(session)}`,
+        {
+          cache: 'no-store',
+          credentials: 'omit',
+        },
+      );
+      if (!response.ok)
+        throw new Error(`Evidence request failed (${response.status})`);
+      const filename =
+        response.headers.get('X-Winscope-Filename') ??
+        'aps-winscope-evidence.zip';
+      const file = new File([await response.blob()], filename, {
+        type: 'application/zip',
+      });
+      await this.onFilesUploaded([file]);
+      await this.onViewTracesButtonClick(true);
+    } catch (error) {
+      console.error(
+        'Unable to load Android Performance Studio evidence',
+        error,
+      );
+      const message = error instanceof Error ? error.message : `${error}`;
+      UserNotifier.add(
+        new PackagedEvidenceLoadFailure(message),
+      ).notify();
+    }
   }
 
   ngAfterViewChecked() {
@@ -980,3 +1011,19 @@ export class AppComponent implements WinscopeEventListener {
     this.downloadRequest(url, filename);
   }
 }
+
+class PackagedEvidenceLoadFailure implements UserNotification {
+  constructor(private readonly message: string) {}
+
+  getNotificationType(): NotificationType {
+    return NotificationType.ERROR;
+  }
+
+  getDescriptor(): string {
+    return 'Android Performance Studio evidence failed to load';
+  }
+
+  getMessage(): string {
+    return this.message;
+  }
+}
diff --git a/winscope/src/app/components/timeline/timeline_component.ts b/winscope/src/app/components/timeline/timeline_component.ts
index 0b7a15592..b044ea9dc 100644
--- a/winscope/src/app/components/timeline/timeline_component.ts
+++ b/winscope/src/app/components/timeline/timeline_component.ts
@@ -413,9 +413,10 @@ import {globalConfig} from 'common/global_config';
       .time-selector-form mat-form-field {
         display: flex;
         width: 100%;
-        font-size: 12px;
+        font-size: 8px;
       }
       .time-selector-form input {
+        font-size: 8px;
         text-overflow: ellipsis;
       }
       .time-selector-form .time-difference {
diff --git a/winscope/src/common/analytics.ts b/winscope/src/common/analytics.ts
index acaac639c..634953507 100644
--- a/winscope/src/common/analytics.ts
+++ b/winscope/src/common/analytics.ts
@@ -28,7 +28,7 @@ export function analyticsLogEvent(
   eventName: Gtag.EventNames | (string & {}),
   eventParams?: Gtag.ControlParams | Gtag.EventParams | Gtag.CustomParams,
 ) {
-  if (globalConfig.MODE === 'PROD') {
-    gtag('event', eventName, eventParams);
-  }
+  void globalConfig;
+  void eventName;
+  void eventParams;
 }
diff --git a/winscope/src/index.html b/winscope/src/index.html
index fffe5648e..ba27af6b6 100644
--- a/winscope/src/index.html
+++ b/winscope/src/index.html
@@ -21,16 +21,6 @@
     <base href="./" />
     <meta name="viewport" content="width=device-width, initial-scale=1" />
     <link rel="icon" type="image/x-icon" href="logo_page_tab.svg" />
-    <!-- Google tag (gtag.js) -->
-    <script async src="https://www.googletagmanager.com/gtag/js?id=G-95DPZ8JGEP"></script>
-    <script>
-      window.dataLayer = window.dataLayer || [];
-      function gtag() {
-        dataLayer.push(arguments);
-      }
-      gtag("js", new Date());
-      gtag("config", "G-95DPZ8JGEP");
-    </script>
   </head>
   <body class="mat-app-background">
     <app-root></app-root>
diff --git a/winscope/src/material-theme.scss b/winscope/src/material-theme.scss
index d015feb60..28258e04b 100644
--- a/winscope/src/material-theme.scss
+++ b/winscope/src/material-theme.scss
@@ -17,12 +17,47 @@
 @use 'sass:map';
 @use '@angular/material' as mat;
 
-@import 'https://fonts.googleapis.com/icon?family=Material+Icons';
-@import '//fonts.googleapis.com/css2?family=Google+Sans';
-@import 'https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0';
+@font-face {
+  font-family: 'Material Icons';
+  font-style: normal;
+  font-weight: 400;
+  src: url('./fonts/MaterialIcons-Regular.ttf') format('truetype');
+}
+
+@font-face {
+  font-family: 'Material Symbols Outlined';
+  font-style: normal;
+  font-weight: 400;
+  src: url('./fonts/MaterialSymbolsOutlined.ttf') format('truetype');
+}
+
+.material-icons,
+.material-symbols-outlined {
+  font-weight: normal;
+  font-style: normal;
+  font-size: 24px;
+  line-height: 1;
+  letter-spacing: normal;
+  text-transform: none;
+  display: inline-block;
+  white-space: nowrap;
+  word-wrap: normal;
+  direction: ltr;
+  font-feature-settings: 'liga';
+  -webkit-font-feature-settings: 'liga';
+  -webkit-font-smoothing: antialiased;
+}
+
+.material-icons {
+  font-family: 'Material Icons';
+}
+
+.material-symbols-outlined {
+  font-family: 'Material Symbols Outlined';
+}
 
 $typography: mat.m2-define-typography-config(
-  $font-family: 'Roboto, sans-serif',
+  $font-family: 'Arial, sans-serif',
   $headline-1:
     mat.m2-define-typography-level(
       $font-size: 24px,
diff --git a/winscope/src/trace_processor/perfetto/engine.ts b/winscope/src/trace_processor/perfetto/engine.ts
index f521e8151..3da1bc678 100644
--- a/winscope/src/trace_processor/perfetto/engine.ts
+++ b/winscope/src/trace_processor/perfetto/engine.ts
@@ -219,22 +219,27 @@ export abstract class EngineBase implements Engine {
     };
 
     const rpc = perfetto.protos.TraceProcessorRpc.decode(rpcMsgEncoded);
+    const sequenceId = Number(rpc.seq);
 
     if (rpc.fatalError !== undefined && rpc.fatalError.length > 0) {
       this.fail(`${rpc.fatalError}`);
     }
 
     // Allow restarting sequences from zero (when reloading the browser).
-    if (rpc.seq !== this.rxSeqId + 1 && this.rxSeqId !== 0 && rpc.seq !== 0) {
+    if (
+      sequenceId !== this.rxSeqId + 1 &&
+      this.rxSeqId !== 0 &&
+      sequenceId !== 0
+    ) {
       // "(ERR:rpc_seq)" is intercepted by error_dialog.ts to show a more
       // graceful and actionable error.
       this.fail(
         `RPC sequence id mismatch ` +
-          `cur=${rpc.seq} last=${this.rxSeqId} (ERR:rpc_seq)`,
+          `cur=${sequenceId} last=${this.rxSeqId} (ERR:rpc_seq)`,
       );
     }
 
-    this.rxSeqId = rpc.seq;
+    this.rxSeqId = sequenceId;
 
     let isFinalResponse = true;
 
diff --git a/winscope/src/viewers/viewer_media_based/viewer_media_based_component.ts b/winscope/src/viewers/viewer_media_based/viewer_media_based_component.ts
index 9a63a54e9..b15ccb0a5 100644
--- a/winscope/src/viewers/viewer_media_based/viewer_media_based_component.ts
+++ b/winscope/src/viewers/viewer_media_based/viewer_media_based_component.ts
@@ -160,7 +160,7 @@ import {ViewerEvents} from 'viewers/common/viewer_events';
       }
 
       .overlay-title {
-        font-size: 14px;
+        font-size: 7px;
         width: unset;
       }
 
@@ -198,6 +198,10 @@ import {ViewerEvents} from 'viewers/common/viewer_events';
         padding: 1rem;
         text-align: center;
       }
+
+      .no-video .mat-body-2 {
+        font-size: 7px;
+      }
     `,
   ],
 })
diff --git a/winscope/src/viewers/viewer_surface_flinger/presenter.ts b/winscope/src/viewers/viewer_surface_flinger/presenter.ts
index b59534e37..8f0e7ed75 100644
--- a/winscope/src/viewers/viewer_surface_flinger/presenter.ts
+++ b/winscope/src/viewers/viewer_surface_flinger/presenter.ts
@@ -534,8 +534,7 @@ the default for its data type.`,
     cornerRadiusLabel: string,
   ): string {
     let [tl, tr, bl, br] = ['0', '0', '0', '0'];
-    const radiiNode = assertDefined(tree.getChildByName(cornerRadiiLabel));
-    const radii = radiiNode.getAllChildren();
+    const radii = tree.getChildByName(cornerRadiiLabel)?.getAllChildren() ?? [];
     if (
       radii.length > 0 &&
       radii.every((r) => r.source !== PropertySource.DEFAULT)
diff --git a/winscope/src/viewers/viewer_surface_flinger/presenter_test.ts b/winscope/src/viewers/viewer_surface_flinger/presenter_test.ts
index 275fdb4b6..019285816 100644
--- a/winscope/src/viewers/viewer_surface_flinger/presenter_test.ts
+++ b/winscope/src/viewers/viewer_surface_flinger/presenter_test.ts
@@ -673,7 +673,6 @@ the default for its data type.`,
         const layer2Props = getPropertiesForCuratedPanel(2n);
         Object.assign(layer2Props, {
           cornerRadius: 6,
-          cornerRadii: {tl: 0, tr: 0, bl: 0, br: 0},
           requestedCornerRadii: {bl: 3},
         });
 
diff --git a/winscope/webpack.config.prod.js b/winscope/webpack.config.prod.js
index 89dba44fc..2ca179363 100644
--- a/winscope/webpack.config.prod.js
+++ b/winscope/webpack.config.prod.js
@@ -18,6 +18,8 @@ const configCommon = require('./webpack.config.common');
 const path = require('path');
 const HtmlWebpackPlugin = require('html-webpack-plugin');
 const AngularWebpackPlugin = require('@ngtools/webpack').AngularWebpackPlugin;
+const LicenseWebpackPlugin =
+  require('license-webpack-plugin').LicenseWebpackPlugin;
 
 const configProd = {
   mode: 'production',
@@ -66,7 +68,17 @@ const configProd = {
       template: 'src/index.html',
       inject: 'body',
     }),
+    new LicenseWebpackPlugin({
+      perChunkOutput: false,
+      outputFilename: 'third-party-licenses.txt',
+    }),
   ],
 };
 
-module.exports = merge(configCommon, configProd);
+const mergedConfig = merge(configCommon, configProd);
+mergedConfig.devtool = false;
+mergedConfig.module.rules = mergedConfig.module.rules.filter(
+  (rule) => rule.loader !== '@ephesoft/webpack.istanbul.loader',
+);
+
+module.exports = mergedConfig;
