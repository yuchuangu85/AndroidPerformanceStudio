# Use an in-app read-only source viewer as the navigation target

Resolved source locations open in a consistent in-app read-only viewer that displays snapshot identity, path, line range, supporting evidence, and resolution confidence. Local sources may additionally open in an IDE and remote sources in their provider browser, but those integrations are optional actions rather than the canonical navigation path. This preserves historical and cross-provider behavior without turning Android Performance Studio into a source editor.
