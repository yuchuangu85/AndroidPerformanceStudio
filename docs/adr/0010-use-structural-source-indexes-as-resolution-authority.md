# Use structural source indexes as the source resolution authority

The first source index is built from files, modules, packages, resources, language symbols, native Build IDs, and source-line metadata. Text search may broaden discovery, while future embeddings may produce weak candidates only; neither semantic similarity nor model output can authorize direct navigation. This delays broad natural-language code search in favor of explainable and testable source resolution.
