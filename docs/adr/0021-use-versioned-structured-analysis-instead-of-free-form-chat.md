# Use versioned structured analysis instead of free-form chat

The first release produces schema-validated findings whose evidence and source references are explicit, and reruns create new analysis versions when scope, source bindings, or model settings change. It does not expose an unrestricted chat surface because follow-up prose could escape the deterministic candidate boundary and weaken reproducibility. A future follow-up workflow must remain session-bound and preserve structured evidence references.
