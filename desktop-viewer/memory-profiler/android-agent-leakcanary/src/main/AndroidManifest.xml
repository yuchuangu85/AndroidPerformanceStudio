<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="com.androidperformancestudio.memory.leak.agent.LeakCanaryBridgeProvider"
            android:authorities="${applicationId}.aps.leakcanary.bridge"
            android:exported="false"
            android:initOrder="100" />
    </application>
</manifest>
