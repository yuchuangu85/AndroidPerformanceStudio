# AOSP UI Inspector protocol provenance

`ui_inspector.proto` and `view_inspector.proto` are unmodified copies from the
vendored `third_party/aosp-ui-inspector/common/proto` snapshot pinned in its
`PROVENANCE.md` (AOSP tools/base commit
`b1261356012800c0a93d18f03b060024e8162c2f`). Licensed under Apache-2.0.
