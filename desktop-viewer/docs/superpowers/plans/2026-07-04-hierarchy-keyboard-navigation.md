# Hierarchy Keyboard Navigation Implementation Plan

1. Add failing state tests for Up/Down and Enter behavior.
2. Add pure visible-row navigation and expandable-row toggling to `HierarchyTreeState`.
3. Make the hierarchy pane focusable and request focus on mouse selection.
4. Handle Up, Down, and Enter key-down events and keep keyboard selection visible.
5. Run desktop and full-project tests, then restart the application.
