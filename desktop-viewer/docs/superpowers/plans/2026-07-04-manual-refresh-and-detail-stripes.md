# Manual Refresh and Detail Stripes Implementation Plan

1. Add failing tests for scan-control state and property stripe parity.
2. Add one-shot capture request and progress state alongside the auto-scan loop.
3. Draw and display the refresh icon only in manual mode.
4. Add deep/light detail-row palette colors and apply them by row index.
5. Run desktop and full-project tests, then restart the application.
