import { describe, expect, it } from 'vitest';
import { decodeViewInspection, encodeDumpViewsCommand } from './view-inspector-protocol.js';
const v=(x:number):number[]=>{const a:number[]=[];do{const b=x&127;x>>>=7;a.push(x?b|128:b)}while(x);return a}; const c=(x:Uint8Array[])=>Uint8Array.from(x.flatMap(y=>[...y])); const f=(n:number,x:Uint8Array)=>Uint8Array.from([...v(n<<3|2),...v(x.length),...x]); const i=(n:number,x:number)=>Uint8Array.from([...v(n<<3),...v(x)]);const s=(x:string)=>new TextEncoder().encode(x);
describe('View inspector protocol',()=>{it('decodes root ids, strings, bounds and raw properties',()=>{const e=(id:number,x:string)=>f(2,c([i(1,id),f(2,s(x))]));const rect=c([i(1,10),i(2,20),i(3,30),i(4,40)]);const attr=c([i(1,3),i(2,1),i(101,4)]);const node=c([i(1,77),i(3,1),f(5,rect),i(7,2),f(9,attr)]);const dump=c([f(1,node),e(1,'android.widget.TextView'),e(2,'pkg:id/title'),e(3,'text'),e(4,'Hello')]);const result=decodeViewInspection(f(1,dump),'pkg',5);expect(result.rootViewIds).toEqual([77]);expect(result.snapshot.root).toMatchObject({id:'view:77',className:'android.widget.TextView',resourceName:'pkg:id/title',bounds:{left:10,top:20,right:40,bottom:60},attributes:{rawProperties:{text:'Hello'}}});});it('encodes dump command',()=>expect([...encodeDumpViewsCommand(true)]).toEqual([0x0a,0x04,0x08,0x01,0x10,0x00]));});
